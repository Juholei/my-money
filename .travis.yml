addons:
  postgresql: "9.5"
sudo: required
language: clojure

jdk:
  - openjdk8
  
before_install:
  - sudo apt-get install postgresql-9.5
  
before_script:
  - psql -c 'create database travis_ci_test;' -U postgres

script:
  - lein do test, fronttests-once
  
